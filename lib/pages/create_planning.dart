import 'package:flutter/material.dart';

class CreatePlanning extends StatelessWidget {
  const CreatePlanning({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Create Planning'),
      ),
    );
  }
}
